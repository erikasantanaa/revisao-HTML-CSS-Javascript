const button = document.querySelector('button');
const popup = document.querySelector('.popup-close');
const cleseButton = document.querySelector('popup-close');

button.addEventListener('click', event => {
    
})